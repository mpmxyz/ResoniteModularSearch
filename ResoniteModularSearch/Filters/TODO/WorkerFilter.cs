namespace ResoniteModularSearch.Filters.TODO;

/// <summary>
/// idea: all filters are very primitive and only match the kind of object they are looking for
/// This filter would match slots, components, etc. that contain properties which match the given expressions
/// TODO: how would this prevent unnecessary recursion and keep track of intermediate matches for replace operations?
/// </summary>
internal class WorkerFilter {
    //
}
